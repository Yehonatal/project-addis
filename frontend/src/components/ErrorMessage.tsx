import {
    ErrorContainer,
    ErrorContent,
    ErrorIcon,
    ErrorText,
    CloseButton,
} from "@/styles/component-styles/ErrorMessage.style";

const ErrorMessage = () => {
    return <>ErrorMessage</>;
};

export default ErrorMessage;
